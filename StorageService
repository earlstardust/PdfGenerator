public class StorageService : Interface // Interface for storage service at hand. Can be change if desired
{
    // variables
    
    // methods
    
    public Task<T> Save(Document document)
    {
        ...
    }
}
